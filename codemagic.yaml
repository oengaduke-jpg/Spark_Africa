workflows:
  spark-africa-workflow:
    name: Spark Africa Build
    max_build_duration: 60
    environment:
      node: latest

    scripts:
      - npm install
      - npm run build

    artifacts:
      - dist/**

    publishing:
      email:
        recipients:
          - you@example.com
